# aiteru-docker

## Getting Started

### up

```
$ docker-compose up -d
$ curl localhost:2000 # for browser use
$ curl localhost:8000 # backend server
```

### down

```
$ docker-compose down
$ docker-compose rm
```
